# Optional provider configuration for future adapters. Keep values empty locally.
# Provider keys must never be committed and are not read by the default app.
MARKET_DATA_PROVIDER_KEY=
INSTRUMENT_SEARCH_PROVIDER_KEY=
